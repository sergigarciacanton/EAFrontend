import 'package:flutter/material.dart';

class SettingsProfile extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text('im settings and user profile page');
  }
}
